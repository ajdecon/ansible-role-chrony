---
chrony_pkg_state: present
chrony_service_state: started
chrony_service_enabled: yes
chrony_config_server:
  - 0.pool.ntp.org
  - 1.pool.ntp.org
  - 2.pool.ntp.org
  - 3.pool.ntp.org
chrony_config_logdir: /var/log/chrony
chrony_timezone: Etc/UTC

# Add servers to chrony_config_preferred if you want the servers to have the
# "prefer" directive appended in config. They must also appear in
# chrony_config_server.
#chrony_config_preferred:
# - 0.pool.ntp.org
